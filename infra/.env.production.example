# Infrastructure production environment example
# Copy this file to .env and fill in secure values before running `docker compose up -d --build`

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=app_db
POSTGRES_PORT=5432

# Backend DB connection (service name `postgres` inside docker network)
DATABASE_URL=postgres://postgres:postgres@postgres:5432/app_db

# Public domain for HTTPS (Caddy will issue certificates for this domain)
DOMAIN=jdimpresions.shop

# Backend app settings
PORT=4000
JWT_SECRET=change-me
JWT_EXPIRES_IN=8h
ADMIN_BOOTSTRAP_KEY=change-this-bootstrap-key

# Email via Gmail API (used by backend/src/utils/email.js)
GMAIL_USER=your_account@gmail.com
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret
GMAIL_REFRESH_TOKEN=your_refresh_token
EMAIL_FROM="Papeleria <no-reply@jdimpresions.shop>"

# Optional: reCAPTCHA secret if used on frontend
# RECAPTCHA_SECRET=
